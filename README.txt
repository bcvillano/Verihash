Verihash

Python script for validating file hashes by comparing the expected hash to the actual hash, utilizing a csv file of file paths and expected file hashes

Note: example.csv can be used to test this tool, the first 3 test files should show up as good but the last test should fail

